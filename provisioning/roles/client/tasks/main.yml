---
# This is a role for setting up the client.

- name: Update apt cache
  apt:
    update_cache: yes

- name: Install packages
  apt:
    pkg:
     - hydra
    update_cache: yes
  ignore_errors: True
...
